---
# Install dependencies
- name: RedHat - Install Package Dependencies
  become: true
  yum:
    update_cache: true
    state: present
    name: "{{ item }}"
  with_items: "{{ redhat_dependencies }}"